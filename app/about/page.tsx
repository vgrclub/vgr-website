import React from 'react'
//TODO: this will be the about page, when creating endpoints for pages just create a folder in app dir, name it the page, and create a page.tsx like here
const page = () => {
  return (
    <main className="flex min-h-screen flex-col items-center justify-between p-24">
      about content
    </main>
  )
}

export default page